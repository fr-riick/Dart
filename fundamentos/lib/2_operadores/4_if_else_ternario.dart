void main(){
  
  // Operador ternário serve pra fazer um if_else em uma linha só

  const idade = 17;

  // idade >= 18 ? print('É maior de idade') : print('Não é maior de idade');

  // final eMaiorIdade = idade >= 18 ? true : false;
  // print((eMaiorIdade) ? 'é maior de idade' : 'é menor de idade');

  print(idade > 17  ? "é maior de idade" : "é menor de idade");

}