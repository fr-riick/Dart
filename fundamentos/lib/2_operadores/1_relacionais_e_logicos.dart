void main() {
  // operadores relacionais
  // == (igualdade), != (diferença), > (maior que), < (menor que), >= (maior ou igual), <= (menor ou igual)

  // Operadores lógicos
  // &&(and), ||(or), !(not)
}