
void main(){

  var number = 0;

  // while convencional
  while (number < 3) {
    print(number);
    number++;
  }

  // Do-While
  // Executa uma vez antes de verificar a condição
  var index = 0;
  do {
    print(index);
  } while (index > 0);
}